---
epic: 13
date: 2026-09-22
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 13 Retrospective — Engine Correctness & the Supervisor Module Boundary (2026-09-22)

## Epic summary

- **Epic:** Epic 13 — Engine Correctness & the Supervisor Module Boundary (opened 2026-09-19 to close the two HIGH retro defects and the twice-carried split study; all paths post-rename `crates/hekma-*`).
- **Diff range:** `7f4454e^..7f4454e` — the PR #192 squash on main (commit `7f4454e`, "sprint-record closeout + epic-13 (supervisor split A+B implemented)"). One commit by construction; per-story attribution taken from tracker rows (narrowing recorded). Note the code split and the sprint-record closeout rode ONE squash — churn mixing a batch-epic precedent carries; recorded as a minor process observation, accepted.
- **Stories:** 13-1, 13-2, 13-3 — all `done` (`detect-epic --epic 13`: `story_count: 3`, `pending_stories: []`). No retrospective existed before this run.
- **Evidence inventory:** epic spec (epics.md, with the 13-1/13-2 Resolution notes); story specs (`13-1/2/3` files under `_bmad-output/implementation-artifacts/`); full squash diff (19 files, via `git_evidence.py`); `supervisor-split-study-2026-09-19.md` (13-3's deliverable); sprint status rows. **Missing:** session logs (skipped), per-story commit boundaries on main (narrowed), no fresh runtime exercise at retro time.
- **Volume shape:** the split itself — `crates/hekma-engine/src/domain/supervisor.rs` −9,101 (monolith removed) becomes `domain/supervisor/{mod.rs +1,396, usage.rs +943, reaper.rs +772, spawn.rs +686, lifecycle.rs +686, interaction.rs +353}` plus `supervisor/tests.rs` +4,318; the study doc +115; tracker +66/−66.

## Findings

### Aggregate views

- **Architecture delta — the epic's purpose, delivered.** The 9,101-line `supervisor.rs` monolith (the F-agg2 / AD-18 carried debt since epic-11) is gone; supervision now lives in six focused modules (usage / reaper / spawn / lifecycle / interaction under a `mod.rs`), and the engine crate's public surface did not change (no semver event recorded for the split — it is internal restructuring). The boundary followed the ratified study (`supervisor-split-study-2026-09-19.md`), with implementation gated on Islam's ratification per AI-58 and the PR title recording "split A+B implemented" as that ratification.
- **God-class check — one watch item (defer).** The split moved the test suite as ONE file: `supervisor/tests.rs` at +4,318 lines is now the engine's largest test module. The production boundary is modular; the test boundary is not. Disposition: defer — any future test split can follow the new module seams; no action item now (cohesion is per-behavior-family, and splitting tests is mechanical once wanted).
- **Duplication map — clean.** The split relocated, not duplicated: net non-test production volume (≈4,100 lines across six modules) is consistent with the removed monolith's supervision sections; no helper was reimplemented per-module.
- **Pattern divergence — none found.** Module layout (`domain/supervisor/*`) matches the existing `domain/` convention; error and test idioms carried over.

### Spec-to-implementation reconciliation — the retro-honesty positive

Stories 13-1 and 13-2 both resolved as VERIFICATION stories, not rebuilds, because the defects they targeted were already closed by earlier work — recorded in the epic spec's own Resolution notes: 13-1's delivery defect was closed by story 11-3 (`supervisor.rs` override-branch strip, mutation-verified pin at `tests/observed_metering.rs:826`); 13-1's remaining truth (the LOCAL-path strip shipped unpinned) landed as a mutation-verified, positive-controlled pin (`tests/memory.rs::a_hand_set_memory_dir_never_reaches_a_plain_start_surfaces`). 13-2 likewise reconciled against the legacy-fallback gate (story spec + tracker row). This is the AI-19 discipline (prove, don't assume) working as designed — the retro records it so later runs don't re-flag the "missing strip" as open.

### Diff-scope review (narrowed, recorded)

No fresh `bmad-review` pass at retro time. Narrowing: story-time reviews stand in the record (tracker rows 13-1/13-2/13-3; the 13-1 pin is mutation-verified with a positive control); this retro ran the aggregate views plus a boundary spot-check that the module split introduced no cross-module reach-in beyond the declared `mod.rs` re-exports. Residual risk: none identified.

### Behavior verification

Not re-exercised at retro time. Story-time verification: gates green at the epic's merge (tracker rows); the split is behavior-preserving by construction (move + re-export) with the existing supervisor suite passing unchanged in place (`supervisor/tests.rs`).

## Previous-retro follow-through

Previous retro: epic-12 (`epic-12-retro-2026-09-19.md`) — items 95–99, all `status: open` in the ledger. Per-item (proposals only; headless does not write):

- **epic-12-retro-item-95 (macOS CI flake family)** — no evidence found of stabilization work; still on the post-epic menu. Proposed status: none (stays open).
- **epic-12-retro-item-96 (tracker-status consistency pin in test_automation.py)** — no evidence found; `scripts/test_automation.py` carries no tracker-status test (grep, this branch). Proposed status: none (stays open).
- **epic-12-retro-item-97 (canonize verify-the-diff-not-the-message)** — no evidence found; the AGENTS.md Engineering-patterns section does not name it. Proposed status: none (stays open).
- **epic-12-retro-item-98 (two Windows lifetime caveats on operator surfaces)** — PARTIALLY landed: `docs/troubleshooting.md` now carries the unsupervised-window honesty (line 51), the ACP detach transport caveat (line 58), and the `--detach` refusal section (lines 64–66); the Windows capture-hang caveat (captured `start --detach` output hangs on the inherited pipe write-ends) is NOT yet on any operator surface. Proposed status: `in-progress`.
- **epic-12-retro-item-99 (c6ecadb seam test or documented accept)** — the documented-accept route appears satisfied: `crates/hekma-engine/src/backends/windows/mod.rs:685-690` documents the SYNCHRONIZE right as NOT optional, naming the story 12-1 review fix; no dedicated seam test found. Proposed status: `done` (documented-accept route), Islam to confirm.

## Action items

1. **[process] Ledger drift sweep (shared — ledgered on epic-14's retro).** As recorded in epic-10's retro. Owner: Islam (confirm) + agent (prepare).
2. **[defer, recorded] `supervisor/tests.rs` monolith watch.** If a future change touches supervision tests broadly, split the file along the new module seams then. Owner: next story touching the file. (Recorded, no item created.)

## Acceptance verdict

**accepted-with-open-items** (criteria: declared). All three stories `done`; 13-1 and 13-2 closed their defects or their verification truth with mutation-verified pins (the spec's own Resolution notes are the reconciliation record); 13-3 delivered the bounded-options study and the ratified implementation (split A+B) without changing the public surface. Open items are prior-epic carry-overs (95–99), tracked and dispositioned above. Verified against: the squash diff, the split study, the epic spec Resolution notes, tracker rows.

## Open questions

- Islam's confirmation on item 99's `done` proposal (documented-accept route) and item 98's `in-progress` — the retro proposes, only he confirms.

## Assumptions (headless)

- Epic selected by explicit invocation (epic-13, second of the three owed retros).
- `detect-epic --epic 13` returned `pending_stories: []`.
- Verdict rendered with no human at the console: accepted-with-open-items.
- Follow-through statuses for epic-12 items 95–99 are PROPOSALS with sources; not written to the ledger (headless rule).
- Diff-scope review narrowed as recorded.
