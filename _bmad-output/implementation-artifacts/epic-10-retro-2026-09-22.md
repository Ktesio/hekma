---
epic: 10
date: 2026-09-22
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 10 Retrospective — Consolidate & Harden the Embedding Surface (2026-09-22)

## Epic summary

- **Epic:** Epic 10 — Consolidate & Harden the Embedding Surface (opened 2026-09-10 by Islam-approved sprint change proposal `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-10.md`).
- **Diff range:** `b590dc8^..b590dc8` — the PR #180 one-PR squash on main (2026-09-10, commit `b590dc8`). One commit by construction (repo squash-merges), so per-story commit attribution on main is not derivable; story attribution is taken from the tracker rows (each records its story's content and review loop), recorded as a narrowing.
- **Stories:** 10-1, 10-2, 10-3 — all `done` (`detect-epic --epic 10`: `story_count: 3`, `pending_stories: []`). No retrospective existed before this run.
- **Evidence inventory:** epic spec (epics.md, declared per-story AC); story specs (`spec-10-1/2/3-*.md`); full squash diff (27 files, via `git_evidence.py`); sprint status rows; deferred-work.md entries from the epic-7 suites. **Missing:** session logs (process-lesson analysis from them skipped), per-story commit boundaries on main (narrowed to tracker attribution), no fresh runtime exercise at retro time (see Behavior verification).
- **Volume shape:** `crates/ktesio-conformance/src/test_support.rs` +924 (consolidation home); `crates/ktesio-engine/src/domain/resync.rs` +761; `tests/diagnostic_sink.rs` +746; `tests/resync_events.rs` +574; `tests/events_subscription.rs` −162 (dedup); `tests/agent_cli.rs` −142 (dedup); `src/engine.rs` +196 (DiagnosticSink + resync plumbing); `src/domain/supervisor.rs` +150.

## Findings

### Aggregate views

- **Duplication map (the epic's core AC) — met for the embedding suites.** `test_support.rs` (+924) is now the one home for manifest builders + drains; the two worst duplicates shrank (`events_subscription.rs` −162, `agent_cli.rs` −142). The embedding quickstart stays deliberately standalone per the story's own carve-out. Residual: the census update in `deferred-work.md` (Epic-7 review, 2026-09-09) still names ~5 near-identical manifest writers outside the consolidation (events_subscription trio was folded by this diff; the perf-budgets harness + deliberately-standalone quickstart writers remain) — tracked as the open "consolidate the remaining duplicate manifest-writer fixtures" item. Disposition: defer (tracked).
- **Architecture delta — additive only.** No layering change: `DiagnosticSink` and `resync_events` enter through the existing engine facade (`engine.rs` +196); no new cross-cutting dependencies; boundary graph unchanged (10-1's declared AC).
- **God-class growth — none.** New files are focused (resync.rs 761 lines is one mechanism; the sink tests are tests). No file grew past healthy size over the range.
- **Spec-to-implementation reconciliation — one record gap (fix now).** Story 10-2 shipped the host-provided diagnostic sink and emptied the 7-3 audit allowlist (tracker row 10-2; `tests/embed_clean.rs` +171 pins the empty allowlist), which RESOLVES the `deferred-work.md` entry "A Host-provided diagnostic sink — the closure for the TWO allowlisted AD-12 diagnostics" — but that entry still carries no `resolved:` marker line, so the file's own "still open = absence of `resolved:`" convention misreports it as open. (The sibling 7-2 and 7-5 entries carry their `resolved:` lines from 10-3 — the inconsistency is 10-2's entry only.) Disposition: fix now (doc sweep).

### Diff-scope review (narrowed, recorded)

A fresh `bmad-review` pass was not run at retro time. Narrowing: every story carried its own three-lens review at dev time with patches applied (tracker rows 10-1/10-2/10-3 record the loops and the zero-assertion-change consolidation guarantee); this retro ran the aggregate views above plus boundary spot-checks (the sink's default-stderr byte-identity seam, the resync backfill-then-subscribe ordering) against those recorded review artifacts. Residual risk of that narrowing: none identified beyond the tracked deferrals.

### Behavior verification

Not re-exercised at retro time. Story-time verification stands in the record: 1119 tests green, tarpaulin 95.26%, all 16 CI checks at merge (tracker rows 10-1..10-3); the subscriber budgets are GATED in the perf harness from observed measurements, not asserted (tracker row 10-3).

## Previous-retro follow-through

Previous retro: epic-9 (`epic-9-retro-2026-07-14.md`). Its action items AI-54, AI-55, AI-56, AI-57, AI-58 are all `status: done` in `sprint-status.yaml` — nothing outstanding to follow through on. No `--set-action-status` proposals arise from this section.

## Action items

1. **[process] Ledger drift sweep (shared with epics 13/14 — ledgered on epic-14's retro to avoid duplicate tracking).** Many action items that demonstrably landed in epic-11's batches still read `status: open` in `sprint-status.yaml` (e.g. AI-7/8/9/12/13/14/15/16/41/44 → tracker row 11-1; AI-24/26/27/28/33/39 → row 11-2; AI-18/19/21/22/25/30/34/43/45 → row 11-4; AI-48 → row 11-6). Proposed `--set-action-status` transitions are listed in epic-14's retro; headless run does not write them. Owner: Islam (confirm) + orchestrating agent (prepare the batch).
2. **[fix-now, doc] deferred-work.md `resolved:`-marker hygiene.** Append `resolved: story 10-2 (PR #180 / b590dc8) 2026-09-10` to the 7-3 diagnostic-sink entry, and sweep the file for any other shipped-but-unmarked entry so "still open" stays greppable. Owner: agent. (Proposed here; applied in the retros PR.)

## Acceptance verdict

**accepted-with-open-items** (criteria: declared). Every story `done`; each declared AC met in evidence: 10-1 no duplicate builder/drain logic outside the support module (embedding suites), boundary graph unchanged; 10-2 sink shipped, both allowlist entries removed, additive + documented; 10-3 resync shipped with crash-window simulation tests and the subscriber budget ratified from measurement and gated. Open items are tracked deferrals (residual fixture writers, TCK hardening family), none blocking. Verified against: the squash diff, tracker rows, deferred-work.md.

## Open questions

- Should the perf-budgets harness writer and the quickstart writer be folded into `test_support` after all (the quickstart's standalone-ness was a deliberate 7-4 property, and the AC preserves it)? Islam's call if he wants the last consolidation mile.

## Assumptions (headless)

- Epic selected by explicit invocation (epic-10, the first of the three owed retros directed by Islam on 2026-09-22).
- `detect-epic --epic 10` returned `pending_stories: []` — machine verdict not forced to rejected.
- Verdict rendered with no human at the console: accepted-with-open-items, from declared criteria + tracked open items.
- The two proposed action items above, and the shared ledger-drift batch recorded on epic-14's retro, are proposals awaiting human confirmation; nothing outside this document and the retros PR was written.
- Diff-scope review narrowed as recorded (no fresh bmad-review pass; story-time reviews stand in the record).
