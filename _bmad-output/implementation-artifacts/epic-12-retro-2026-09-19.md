---
epic: 12
date: 2026-09-19
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 12 Retrospective — Durable Detach & the Production-Usable Observed Channel (2026-09-19)

## Epic summary

**Epic:** 12 — Durable Detach & the Production-Usable Observed Channel (stories 12-1…12-4, ratified 2026-09-15 from `ai-20-ai-47-product-calls-2026-09-15.md`).
**Diff range:** `49da96b^..4299bde` — 18 commits, no merges (the epic itself is ONE squash, 49da96b = PR #184, merged 2026-09-15 07:48). Post-merge tail: 8842b8b (semver baseline moves + tracker), org-migration chores (range noise, not epic work), c6ecadb (Windows adopted-stop SYNCHRONIZE fix), 394e660 (engine 0.3.0 bump — the epic's breaking release), the Windows detach-test triage series (08ae1d5→56ee766), a7fcc1b (sprint record), 4299bde (0.3.0 publish recording).
**Stories:** 4/4 `done` (all merged in the squash). `detect-epic --epic 12`: `story_count: 4`, `pending_stories: []` — the completeness gate passed; the machine verdict is not forced.
**Evidence inventory:** epic spec (`spec-epic-12-durable-detach-observed-channel.md`, 6 declared ACs, all tasks ticked, `review_loop_iteration: 1`) ✓; context doc ✓; per-story story files — **NONE exist** (batch-epic shape, same as epics 10–11; the spec + tracker annotations carry per-story intent) — inventory gap recorded; `git_evidence.py` per-file volume ✓; sprint-status ✓; previous retro (epic-11, 2026-09-15) ✓; **session logs unavailable** — process-lesson analysis is narrowed to the durable record (commit series, spec change log, tracker annotations, CI run history).

## Findings

Each finding carries its source. Dispositions: fix now / defer / accept.

### Aggregate views

- **A1 [MED, defer→next engine epic] supervisor.rs growth continues unowned.** `git_evidence.py`: +1052/−59 (net +993) in this range; current file 9101 lines (4773 production before `#[cfg(test)]` at `crates/hekma-engine/src/domain/supervisor.rs:4774`, 4327 test) — on top of epic-11's +36%. The epic-11 retro's split-study item (F-agg2, owner "next engine epic") remains open and epic-12 — itself the next engine epic by chronology — did not fold it in. Disposition: **defer**, carried (already tracked as epic-11 item; not re-added).
- **A2 [LOW, accept] One-PR squash concentrates all review into one pass; per-story commit attribution is weak.** `git_evidence.py` `stories` field: only 3 of 18 range commits name story ids (all post-merge 12-1 triage); the squash carries no per-story subjects. This is the spec'd, ratified shape ("all four stories land together in ONE PR") — recorded as an **accepted divergence**, not a defect. Corollary worth keeping: the loop-1 findings were epic-scope design defects, which is exactly what a single three-layer review pass over one PR is positioned to catch — the shape worked.

### Process / CI evidence

- **P1 [MED, defer→next Windows story] The epic merged before its own Windows leg was proven, and main was red ~15 hours.** CI run history on main (2026-09-15): green at 01:52 (9ee4df7); 49da96b merge at 07:48 (run cancelled, superseded); **8842b8b 07:51 → 56ee766 22:11: eleven consecutive failed CI runs, all `test (windows-latest)`** — the 12-1 detach CLI test hanging; green again at 22:34 (a7fcc1b). The hang was a PRODUCT finding (detached child holds the spawning command's stdio pipe write-ends — capture blocks until agent exit), proven by the forensic harness (56ee766) and routed honestly in deferred-work. Balanced: the test's teeth found a real defect and no dishonesty occurred — but the merge landed with the Windows leg of its own headline AC unproven. Disposition: **defer** (already routed); upstream lesson: an epic whose AC names a per-OS e2e should not merge while that OS leg is unproven (fold into the next Windows story's review checklist).
- **P2 [MED, fix now→action item] macOS CI flake family on main, same-SHA green→red.** 4299bde: CI **success 07:05**, **failure 12:38** — failing `test (macos-latest)`: `uj3_governance_journey_through_documented_cli_commands_unix` (`crates/kt/tests/agent_cli.rs:2977`) and `interleaved_instances_keep_per_instance_fifo` (`crates/hekma-conformance/src/uj3.rs:418`). Recurring 09-18 on `15fcafd`: `log_file_captures_child_stdout` (spawn-race panic at `crates/hekma-engine/src/backends/unix/mod.rs:1042` — "exited immediately, before its process start-time could be read") and `two_engines_in_one_process_drive_the_full_uj3_flow_concurrently_without_collision` (`crates/hekma-engine/tests/embed_clean.rs:323`). The epic added +520 net lines to `agent_cli.rs`, expanding the timing-sensitive e2e surface. Sources: gh run 35097031612 logs; run list 09-15…09-18. Disposition: **fix now** (stabilize under load; fix the race or add principled tolerance) — action item 1.
- **P3 [LOW, fix now→action item] Post-merge tracker flip was claimed but not performed.** 8842b8b's message says "sprint-status.yaml: epic-12 + stories 12-1..12-4 -> done"; the actual diff flips only `epic-12` and appends merge annotations to the four story rows, leaving their status fields `review` (verified via `git show 8842b8b`). The drift stood 2026-09-15→09-19 and made the deterministic status view recommend re-reviewing merged work. Reconciled 2026-09-19 (32ee24f). Disposition: **fix now** (automation pin + pattern) — action items 2 and 3.

### Diff-scope review (bmad-review lenses, delegated independent pass)

An independent agent pass ran the adversarial / edge-case / verification-gap lenses over the full range with the spec's constraints as the rubric (static reading; no test runs — recorded narrowing). Findings re-checked against primary sources before routing; R3 and R4 were independently re-derived in the parent pass before the agent reported them.

- **R1 [MED, defer fix / surface now] Adopted Windows stop cannot reach descendants — the loop-1 asymmetry fix re-opens in the adopted lifetime, caveat lives only in a comment (AI-18).** Source: `49da96b` windows `adopt()` — "there is no documented way to re-open the spawn-time Job Object from a bare pid"; adopted stop arm = `TerminateProcess` on the single opened handle (`crates/hekma-engine/src/backends/windows/mod.rs:661`, verified present at HEAD), while unix adopted stop `killpg`s the tree. The epic's flagship flow (detach → adopt → stop) on Windows therefore kills only the direct child. Not covered by any deferred-work entry, doc, or runtime diagnostic. **Verified in parent pass** (grep at HEAD). Disposition: surface now (action item 4); the kill-parity fix itself defers to the Windows Job-Object work.
- **R2 [MED, surface now] Windows `start --detach` stdio pipe-hold hang is routed internally but absent from every operator-facing surface.** Source: harness proof comment `agent_cli.rs:850-857` at `4299bde`; `docs/troubleshooting.md` carries zero mention (verified: no "pipe" hit; detach sections cover refusal + enforcement window only), and the stderr notice covers enforcement windows only. A scripted capture of the flagship command deterministically hangs. Disposition: **fix now** on the doc/notice surface (action item 4); the underlying fix remains routed to the next Windows story.
- **R3 [LOW, fix now→action items 2+3] 8842b8b's message overstates the tracker change** (= finding P3, independently confirmed by the agent against the same diff).
- **R4 [LOW, fix now→fold into action item 3's story] Spec record integrity: the checked-in spec cites "loop-2" fixes its Spec Change Log never records** — frontmatter `review_loop_iteration: 1` with a Loop-1-only change log, while Suggested Review Order references four "loop-2 fix" items (spec :160/:166/:190/:215), all real in the code (marked "Review round 2"). A second review round's amendments shipped but were never logged. Disposition: **fix now** (small spec-doc reconciliation, owner: next docs-touching change).
- **R5 [LOW, verify] c6ecadb narrows the adoption right-set (adds `PROCESS_SYNCHRONIZE` to `OpenProcess`) with an unexamined failure mode**: a record whose process is openable for query+terminate but whose ACL denies SYNCHRONIZE previously adopted; now `OpenProcess` fails → `Ok(None)` → the supervisor reconciles a LIVE process to `failed`. The commit's belt-and-braces probe fallback documents a shape the same commit makes impossible (all-or-nothing open), so that arm is untestable as shipped. Confidence low-medium (ACL reachability not statically decidable). Disposition: **verify/defer** — fold a seam test (or a documented accept) into the next Windows-touching story; do not treat as a live defect without evidence.

**Checked clean by the pass** (telling checked-and-clean from never-checked): refusal ordering before any side effect with the frozen table touched only by the exit-5 entry; the durable-detach core (schema v6 migration, disarmed re-hold on adopt, restart re-read, record-commit-failure kills the child, stdin force-clear); c6ecadb's wait-first semantics (also fixed adopted crash detection); 12-2/12-3 composition (scheme-agnostic ForwardClient, SSE routing content-type-driven, all five injection gates, rustls confined to `metering/listener.rs`, CI TLS-invariant teeth); 12-4 (same-event retry, break-on-first-error, bounded SKIP, terminal LOST, mode-aware text; the 12-4×12-1 seam guarded by construction since detach refuses engine-observed); consistent semver pin moves; the docs sweep current and honest. Not checked: live Windows observation of R1/R2, real-provider `stream_options` behavior, the audit job's finding state, full line-read of the ~670 added supervisor test lines.

## Behavior verification

Exercised end to end on macOS (2026-09-19, epic-era binary `kt 0.7.0` built 09-15 20:43, i.e. includes the squash + c6ecadb; isolated `KTESIO_STATE_DIR`; sleep-script agent via manifest adapter, self-reported metering):

- `agent register` → honest capability table. `agent start retro-demo --detach` → exit 0, stdout `✓ Started / running`, **stderr carries the enforcement-window notice** ("no crash detection, no budget enforcement, no usage/event delivery between commands") — the spec's hard honesty AC, observed verbatim.
- Child process **alive after kt exited** (`pgrep`: pid 29512). Fresh-engine `kt agent list` → adopted, state `running`; **the benign intervening command did NOT kill the agent** — the loop-1 record-borne-detach amendment holding in practice. `kt agent stop` on the adopted detached handle → `✓ Stopped`, child terminated.
- Refusal path: engine-observed manifest + `--detach` → **exit 5**, reason names the loopback-listener mechanism + remediation, "Nothing was changed", instance still `registered` — refusal before any side effect.
- Not exercised live (CI-proven instead): SSE usage metering e2e and the self-signed-root HTTPS+SSE composition (`tests/observed_metering.rs`, green CI legs), and drain-under-store-failure (supervisor `an_observed_*` suites). Windows legs: CLI detach test passing post-triage (4299bde 07:05 green); engine-level cross-lifetime e2e still runtime-skips on the linux-x86 leg (deferred-work, #109 mitigation).

## Previous-retro follow-through

Prior retro: epic-11 (2026-09-15). Its seven action items (tracker `epic: 11`, ids `epic-11-retro-item-1…7` after the 2026-09-19 dedupe, 32ee24f) — checked against the epic-12 range:

- **item-2's underlying finding (F3, observed-drain lossiness) → LANDED as story 12-4** — `deferred-work.md:126` records the resolution with mechanism and pinning tests. (F3 was a retro finding folded into the epic by ratification, not tracker item 2; item 2 itself — loop-2 invariant tests — shows no landing evidence.)
- **items 1–7: no evidence of landing in the epic-12 range; still `open` in sprint-status.** Epic-12 was the next engine-touching work by chronology but was commissioned fresh from the AI-20/AI-47 product calls and did not fold the epic-11 items. **No status transitions proposed** (none justify `done`; nothing is demonstrably in-progress).

## Action items

Proposed, not applied (retro contract):

1. **[MED, CI stability] Stabilize the macOS flake family (P2).** Same-SHA green→red flips on main: `uj3_governance_journey_through_documented_cli_commands_unix` (`crates/kt/tests/agent_cli.rs:2977`), `interleaved_instances_keep_per_instance_fifo` (`crates/hekma-conformance/src/uj3.rs:418`), `log_file_captures_child_stdout` (`crates/hekma-engine/src/backends/unix/mod.rs:1042` spawn start-time race), `two_engines_…` (`crates/hekma-engine/tests/embed_clean.rs:323`). Reproduce under load; fix the race or add principled tolerance (no blind retry). Owner: next CI-touching story.
2. **[LOW, automation pin] Tracker-status consistency pin (P3).** Extend `scripts/test_automation.py`: fail when any `development_status` row's comment says "MERGED to main" while its status ≠ `done` (would have caught 8842b8b's dropped flip on day zero). Owner: next automation-touching story.
3. **[LOW, process pattern] Canonize "verify the diff, not the message" as an AGENTS.md engineering pattern (AI-57 sibling) (P3/R3).** A commit claiming a bookkeeping/state change (tracker flips, baseline moves, checkbox ticks) is verified against its diff before handoff; the claim in the message is not evidence. Owner: next docs/process-touching change (fold with epic-11 item 3's AGENTS.md edit).
4. **[MED, honesty surface] Put the two Windows lifetime caveats on operator surfaces (AI-18) (R1/R2).** (a) `docs/troubleshooting.md`: a `start --detach` entry for the Windows stdio pipe-hold hang (scripted captures block until agent exit; interactive unaffected) until the routed fix lands; (b) one sentence — doc or the detach stderr notice — stating the adopted-Windows stop boundary (descendants of an adopted detached agent are not terminated; unix `killpg` parity holds only in the spawning lifetime); (c) fold R4: log the second review round in the spec's change log or correct the "loop-2" wording. Owner: next docs-touching change, coordinated with the routed Windows story.
5. **[LOW, verify] c6ecadb seam test (R5).** Pin the adopted-open failure arm (process openable but SYNCHRONIZE-denied → honest `Ok(None)` path, no live-to-`failed` reconciliation) or document the accept, next time the Windows backend is touched. Owner: next Windows-touching story.
6. **Carried, not re-added:** supervisor.rs split study (epic-11 item 7 — owner: next engine epic; file now 9101 lines); Windows stdio pipe-hold fix (deferred-work, routed next Windows-touching story); all other open epic-11 items.

## Acceptance verdict

**accepted-with-open-items** (criteria: **declared** — the spec's six ACs; `pending_stories` empty so no machine rejection).

- **AC-1 (detach survives CLI exit, adoption, usage continuity):** met — live behavior check 2026-09-19 (survive → list adopts → stop, both on the adopted handle) + e2e tests in both layers; Windows-positive CLI counterpart passing post-triage.
- **AC-2 (engine-observed refusal before side effects, exit 5 documented):** met — observed live (exit 5, reason + remediation, "Nothing was changed", state untouched).
- **AC-3 (streamed completion metered exactly once, budgets enforce):** met — `parse_openai_sse_usage` suite + `tests/observed_metering.rs` e2e incl. `--observed-stream-calls`, CI green.
- **AC-4 (https upstream forwards over TLS, usage lands):** met — rustls+ring vendored, self-signed-root composition test, boundary/audit CI jobs green.
- **AC-5 (store failure: park, identical dedup keys, 3-attempt loud SKIP, terminal loss announced):** met — supervisor `an_observed_*` suites per deferred-work.md:126, CI green.
- **AC-6 (gates + one rustls/ring):** met at epic end — full CI success at a7fcc1b (22:34 09-15) with boundary job asserting the single rustls/ring invariant; **caveat recorded in P1**: the Windows leg was red from merge until that hour.

Open items keeping this from plain `accepted`: the six honest deferred-work entries (fleet detach indicator, linux-x86 e2e skip, pause×detach strand, embedding-host zombie reaping, park-loss adoption diagnostic, Windows stdio pipe-hold), the macOS flake family (P2), and the unowned supervisor growth (A1).

## Open questions

1. Should the macOS flake family be a dedicated stability story before the next engine epic (its e2e surface will grow again), or folded into the next CI-touching story?
2. For future batch epics: is a required per-OS "AC leg proven before merge" checklist item wanted in the BMAD review flow (the P1 lesson), or is the current post-merge fix tolerance acceptable given the honest routing?

## Assumptions

Headless run — recorded per contract: (1) epic 12 was selected explicitly in the invocation (no auto-detect); `detect-epic --epic 12` returned `story_count: 4`, `pending_stories: []`. (2) The machine verdict **accepted-with-open-items** was rendered without a human at the console; a human may override it interactively. (3) Action items 1–5 are proposals awaiting owner pick-up; nothing was auto-applied; no `--set-action-status` transitions were written (none of epic-11's items justify `done`). (4) The delegated bmad-review pass completed before finalization and its findings are folded into the diff-scope section above; R3/R4 were independently re-derived in the parent pass, and R1/R2 were spot-checked against HEAD sources before routing. (5) The behavior check ran on the epic-era `kt 0.7.0` binary (built 2026-09-15 20:43, post-c6ecadb) because the v0.8.0 rename has since renamed the CLI package — the artifact is period-correct for this epic.
