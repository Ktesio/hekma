---
epic: 14
date: 2026-09-22
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 14 Retrospective — ACP Backend Support (2026-09-22)

## Epic summary

- **Epic:** Epic 14 — ACP Backend Support (opened 2026-09-19 via Islam's correct-course directive; Sprint Change Proposal `sprint-change-proposal-2026-09-19-acp.md`, APPROVED with decisions D1–D8 including the D2 metering amendment and D7/D8 additions).
- **Diff range:** `1e71389^..1e71389` — the PR #193 one-PR squash on main (merged 2026-09-22 as `1e71389`). Per-story attribution on main is a single squash commit (the subject names only 14-1); story attribution comes from the tracker rows, which record each story's commit on the feature branch (3833c28, a94c9b1, 2684d9e, e9f0f51, 101b6a5, e7dc7fe) — narrowing recorded.
- **Stories:** 14-1, 14-2, 14-3, 14-4, 14-5, 14-6 — all `done` (`detect-epic --epic 14`: `story_count: 6`, `pending_stories: []`).
- **Evidence inventory:** epic spec (`spec-epic-14-acp-backend-support.md`) + epics.md section (declared per-story AC); story/tracker rows; full squash diff (75 files, via `git_evidence.py`); the merge checklist record (baseline moved via PR #229); CodeQL redaction commit fb6f484; Mermaid docs commit 9413f35. **Missing:** session logs (skipped); no fresh runtime exercise at retro time.
- **Volume shape:** the ACP stack — `src/acp/{connection 907, codec 416, client 400, mod 383, updates}` ≈ 2,900 production lines; `tests/acp_lifecycle.rs` +1,402; `ktesio-conformance/src/bin/fake_acp_agent.rs` +562; `store/sqlite.rs` +612 (schema v7 cached tokens + v8 session id); `metering/parse.rs` +305 (tiered usage + cached-token accounting); `crates/hekma/tests/agent_cli.rs` +480 (binary-level e2e); docs sweep + Mermaid wiring (`docs/package-lock.json` +1,248).

## Findings

### Aggregate views

- **Architecture delta — a new backend family, deliberately additive.** The `acp` kind enters as a new `src/acp/` module behind the existing SpawnSpec/kind machinery; the reader runs on a dedicated OS thread per the AD-12 tailer precedent and never takes engine mutexes (tracker row 14-1). The frozen adapter contract v1 is intentionally NOT engaged (builtin kinds don't negotiate — epic-6 retro B3 precedent, recorded in the epic spec). The announced surface breaks (`EngineError::AcpTurnInFlight` exhaustive-enum arm + `SpawnSpec.pipe_stdout` field) went through AI-55's release-surface pass: hekma-engine 0.4.0→0.5.0, banners in RELEASE_NOTES/CHANGELOG, workspace dep req + docs pin bumped in the same commits — and the semver baseline moved post-merge to `1e71389` (PR #229), so the gate is armed for the next break.
- **Duplication map — the metering tiers reuse existing channels rather than forking them.** The observed-channel reuse rides the existing loopback proxy (12-x machinery); the stderr sentinel channel per-channel cursor mirrors the AI-41 park pattern per channel (tracker row 14-3); the fake ACP agent lives in ktesio-conformance next to fake_agent rather than a third copy. No near-identical logic pairs found in the diff.
- **God-class growth — watch, not verdict.** `acp/connection.rs` (907 lines) and `tests/acp_lifecycle.rs` (1,402) are the largest new files; both are cohesive (one transport, one lifecycle suite). No action.
- **Spec-to-implementation reconciliation — honest and recorded.** 14-3's T-finding (14-1 had hard-coded acp = SelfReported, locking it out of the observed channel) was found and fixed in-story via `metering.upstream_base_url` resolution, with the amendment recorded in the tracker and the commit-sweep incident documented (the 2684d9e under-described commit + the `git add -A` ban that came out of it — tracker row and AGENTS-adjacent process record). The CodeQL session-id redaction (fb6f484) enforced the surfaced-not-silent boundary: notes name the OUTCOME, never the opaque token, and tests pin the absence.
- **Docs tooling rode the epic (accepted).** The Mermaid wiring (fumadocs component + `docs/package-lock.json` +1,248/−97) is a docs-build capability, not ACP scope — it landed in the epic's PR. Accepted as batch-epic precedent; noted so nobody reads it as ACP surface.

### Diff-scope review (narrowed, recorded)

No fresh `bmad-review` pass at retro time. Narrowing: every story carried the multi-lens review at dev time (tracker rows record the loops: 14-1 two-pass AI-55 with the CRITICAL unbounded-write-class findings; 14-3's three-lens loop; 14-4's binary-level e2e), plus the epic-level Windows CI stop-ladder fix proven under full-suite parallel load (the scoped-gates lesson). This retro ran the aggregate views above; the between-story seams (transport→sessions→metering→docs) were spot-checked against the tracker's recorded hand-offs. Residual risk: the deferred Windows detached-spawn stdio pipe write-end leak — found BY this epic's own forensic harness (56ee766), tracked in deferred-work.md and the Fibery backlog, routed to the next Windows-touching story.

### Behavior verification

Not re-exercised at retro time; story-time behavioral evidence is strong and specific: three binary-level e2e (missing-command refusal; a full turn landing 40/20/25-CACHED through the real binary with the usage gap cleared; adopted-survivor resume note, unix), 8 acp_lifecycle e2e + ~17 unit tests, Windows-hosted matrix legs green after the stop-ladder fix, and the armed `real_gemini_acp_smoke` (honest skip when absent) — tracker rows 14-1..14-6.

## Previous-retro follow-through

The immediately previous epic (13) has NO retro artifact — `epic-13-retro-*.md` did not exist when this run started (it was produced in this same retros batch, one directory entry earlier). Falling back to the last COMPLETED retro, epic-12 (`epic-12-retro-2026-09-19.md`), items 95–99, all `open`: the per-item record with sources lives in `epic-13-retro-2026-09-22.md` (Previous-retro follow-through) — proposals there: item 98 → `in-progress` (partial: operator caveats on troubleshooting.md:51/58/64; the capture-hang caveat still missing), item 99 → `done` (documented-accept route, `backends/windows/mod.rs:685-690`), items 95/96/97 → no evidence found, stay open. Headless: not written; Islam confirms.

## Action items

1. **[process, fix-now] Run the retro at epic close — the 10/13/14 gap proves the drift.** Three epics completed 2026-09-10/19/20 and none had a retro until this 2026-09-22 batch; epic-13's retro missing entirely broke the follow-through chain (14 had to fall back to epic-12). Change: the epic's merge checklist (the same place the semver-baseline step lives) gains "run bmad-retrospective for this epic" as a step, so the retro is part of closing, not a later sweep. Owner: Islam (ratify) + orchestrating agent (add to the checklist text). (Proposed; applied in the retros PR as the checklist addition.)
2. **[process] Action-item ledger drift sweep — propose the `--set-action-status` batch.** These ledger entries demonstrably landed (sources: tracker rows of the epic-11 batches, `bee7d48`) but still read `open`: AI-7, AI-8, AI-9, AI-12, AI-13, AI-14, AI-15, AI-16, AI-41, AI-44 (row 11-1); AI-24, AI-26, AI-27, AI-28, AI-33, AI-39 (row 11-2); AI-18, AI-19, AI-21, AI-22, AI-25, AI-30, AI-34, AI-43, AI-45 (row 11-4); AI-48 (row 11-6); AI-40 (superseded-by-11-4: the bundle it asked for shipped as the 11-4 docs/process batch); `epic-6-retro-item-6` (docs hygiene — deferred-work entry carries `resolved: … #165 2026-09-05`); `epic-6-retro-item-8` (epic-6 heading reconciled 2026-09-05, tracker line 89). Also `epic-5-retro-item-4` (`resolved: story 11-3 2026-09-11`, deferred-work marker). Proposed statuses: `done` for all of the above; `epic-6-retro-item-5` → `in-progress` (10-1 consolidated the embedding suites; ~5 writers remain per the census). Genuinely-still-open items verified as such: AI-2, AI-4, AI-5 (epic-1 LOWs, no evidence of pickup), AI-32 (Dependabot — now 6 findings flagged on push, up from 4), AI-42, AI-59 (subsumed-by-AI-63(b), still open), `epic-5-retro-item-2` (symlink test exists at `tests/memory.rs:606` but unix-only; UTF-8 delivery still untested), `epic-5-retro-item-3`, `epic-6-retro-item-1`, `epic-6-retro-item-3`, `epic-6-retro-item-4`, `epic-6-retro-item-7`, `epic-12-retro-item-95/96/97`. Owner: Islam (confirm the batch) + agent (run `--set-action-status` on confirmation).
3. **[fix-now, doc] deferred-work.md `resolved:`-marker hygiene (shared with epic-10's retro; the ledger entry lives on epic-10's retro — this doc records the same finding for standalone readability).** The 7-3 diagnostic-sink entry is shipped (10-2) but was unmarked; swept and marked in the retros PR. Owner: agent. (Applied in the retros PR.)
4. **[defer, tracked — no new item] The Windows detached-spawn stdio leak** stays routed where it is (deferred-work.md + the Fibery backlog task "Windows: fix detached-spawn stdio pipe write-end leak"), next Windows-touching story.

## Acceptance verdict

**accepted-with-open-items** (criteria: declared). All six stories `done`; each declared AC met in evidence: transport core with handshake/typed refusals (D1/D3/D6), sessions across lifetimes via the next-start handshake (D4, adopted-processes-not-re-piped ratified), tiered honest usage with the `—`-as-last-resort stance and the context-grain surface (D2 as amended), cached-token accounting with the input-inclusive invariant (D8), hermes deprecation path with parity proven (D7), and the verification/docs sweep including binary-level e2e and six docs pages. The announced engine break went through AI-55 and the semver gate was re-armed post-merge (PR #229). Open items are tracked deferrals (Windows leak, gemini smoke armed-not-run, engine 0.5.0 riding the next publish) plus the process items above — none blocking. Verified against: the squash diff, tracker rows, the merge-checklist record, CodeQL/redaction commits.

## Open questions

- Islam's confirmation on the `--set-action-status` batch (action item 2) — the headless run proposes only.
- Does `hekma-engine 0.5.0` publish together with the held crates-publication decision (the backlog task), or wait for the next deliberate release? Sequencing is Islam's call.

## Assumptions (headless)

- Epic selected by explicit invocation (epic-14, third of the three owed retros directed by Islam on 2026-09-22).
- `detect-epic --epic 14` returned `pending_stories: []`.
- Verdict rendered with no human at the console: accepted-with-open-items.
- All ledger-status changes in action item 2 are proposals with cited sources; nothing was written via `--set-action-status` (headless rule).
- Diff-scope review narrowed as recorded (story-time reviews stand in the record; aggregate views + seam spot-checks run here).
- Per-story commit attribution taken from tracker rows because the main-history squash collapses the branch's story commits (the local feature branch still holds them if a future run needs first-party derivation).
